# SheepShaver Changelog — moved

The SheepShaver changelog has been consolidated into the **fork-wide changelog at the
repository root: [`/CHANGELOG.md`](../CHANGELOG.md)**.

This fork's `macos-arm64` branch modifies both SheepShaver and BasiliskII (plus shared
code), so changes for both now live in one place, component-tagged `[SheepShaver]` /
`[BasiliskII]` / `[shared]` / `[build]` / `[docs]`. The full SheepShaver history was moved
there with `git mv` (preserved). Add new SheepShaver entries to `/CHANGELOG.md`.
